<%- include('partials/header.ejs') %>
        <% if(locals.title){ %>
            <div class="d-flex flex-wrap justify-content-center text-center pt-5 pb-1 mt-5 mb-1">
                <h2><%= title %></h2>
            </div>
            <div class="d-flex flex-wrap justify-content-center text-center py-0 my-4">
                <pre><%= content %></pre>
            </div>
            <div class="d-flex flex-wrap justify-content-center text-center py-0 my-4">
                <a class='btn btn-primary mx-3' href=<%= "editBlog/"+ String(index) %>>Edit Blog</a>
                <a class="btn btn-danger mx-3" href=<%= "deleteBlog/"+ String(index) %>>Delete Blog</a>
            </div>
        <% } %>
<%- include('partials/footer.ejs') %>